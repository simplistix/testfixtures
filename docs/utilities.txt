Utilities
=========

.. _generator:

The ``generator`` helper
------------------------

- generator

The ``wrap`` helper
-------------------

- wrap

  - before
  - after
  - return value
  - arguments (2.5 warning)
  - stacked wrappers  
    - return values
    - less return values than wrappers
  
